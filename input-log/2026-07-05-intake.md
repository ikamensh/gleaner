# 2026-07-05 Intake

The following intake context was provided by the user and is preserved verbatim.

```text
Current intake context:

Latest brief:
**Revised Brief**

Mission:
Gleaner is the durable system of record for coding-agent sessions: capture transcripts, scrub sensitive content before upload, store metadata plus raw transcript safely, and make sessions retrievable for audit, search, and lightweight usage insight.

Current scope:
Claude Code, Cursor, and kodo classification are in scope. Cursor is official same-tier support. Codex capture is in scope for the overall spec, but implementation lands separately from your local `codex-capture` branch; do not build the Codex adapter here.

Next iteration:
Harden the core capture contract around idempotent uploads and safe local acceptance testing.

Acceptance direction:
1. A duplicate `session_id` upload uses last-write-wins semantics.
2. The newer upload replaces transcript and metadata for that `session_id`.
3. Counters, stats, and exports count unique `session_id`s only and must never inflate from duplicate uploads.
4. Mock/local storage and cloud DB storage must agree on duplicate behavior.
5. Default CI/local acceptance tests must prove capture, duplicate replacement, unique-session counting, and raw retrieval without touching production.
6. Live-uploading e2e tests stay behind explicit opt-in only; default CI must not create deployed-service data.
7. Docs should reflect actual supported sources and current behavior: Claude Code and Cursor here; Codex capture pending/separate.
8. Code changes should be PR’d against this fork, `ikamensh/gleaner`, as Hive’s working repo for now.

Likely next steps:
1. Write acceptance stories for capture, duplicate replacement, unique stats/counting, exports, and raw retrieval.
2. Add or adjust tests for mock/local duplicate behavior.
3. Fix storage behavior so duplicate `session_id` replacement is consistent across mock and cloud paths.
4. Gate live production e2e tests behind explicit opt-in.
5. Reconcile README/storage/docs with supported-source reality.

Remaining material questions:
None right now.

Recent transcript:
assistant:
Mission:
Gleaner is the durable system of record for coding-agent sessions: capture transcripts, scrub sensitive content before upload, store metadata plus raw transcript safely, and make sessions retrievable for audit, search, and lightweight usage insight. Current code clearly supports Claude Code and Cursor capture, with kodo classification; Codex-specific upload support was not evident.

Next iteration:
Harden the core capture contract: make upload idempotency safe and tested end-to-end without touching production. A duplicate `session_id` upload should have defined behavior and must not silently inflate counters. Default CI/local acceptance tests should prove capture, duplicate handling, and raw retrieval in mock/local mode.

Likely next steps:
1. Turn today’s promises into acceptance stories for capture, idempotent upload, and retrieval.
2. Decide duplicate-upload semantics: no-op, replace-without-counter-delta, or reject.
3. Fix/test storage behavior across mock and cloud DB paths.
4. Move live production e2e checks behind explicit opt-in so normal test runs never create real data.
5. Reconcile docs with current behavior and supported sources.

Assumptions:
- No `mission.md`, `iteration.md`, or `wiki/` exists here, so the handoff spec is primary.
- “Production care” means not running tests that upload to the deployed service during intake.
- Cursor support is real current scope; Codex support is either future scope or handled outside this repo unless you confirm otherwise.
- PRs are required for code changes; I did not commit or push.

Questions:
1. For duplicate `session_id`, should Gleaner no-op, replace the stored transcript/metadata without changing counters, or reject?
2. Is Codex capture expected to work now from this repo, or should Hive treat it as a missing adapter?
3. Should live tests ever run in CI, or only as a manual production smoke check?

Evidence:
- [README.md](/Users/ikamen/hive-work/gleaner/README.md:3), [DATA_STORAGE.md](/Users/ikamen/hive-work/gleaner/DATA_STORAGE.md:64), [docs/collected-data.md](/Users/ikamen/hive-work/gleaner/docs/collected-data.md:1)
- [gleaner/upload.py](/Users/ikamen/hive-work/gleaner/gleaner/upload.py:38), [gleaner/backfill.py](/Users/ikamen/hive-work/gleaner/gleaner/backfill.py:33), [backend/db.py](/Users/ikamen/hive-work/gleaner/backend/db.py:250), [server/db_mock.py](/Users/ikamen/hive-work/gleaner/server/db_mock.py:375)
- `env -u VIRTUAL_ENV uv run --frozen --with pytest --with httpx python -m pytest -q --ignore=tests/test_e2e.py` passed: `204 passed`.
- `tests/test_e2e.py` was not run because it can upload and leave real server data.

user:
Same answers as the earlier intake round: 1) Duplicate session_id = replace transcript/metadata with the newer upload (sessions grow, last-write-wins), counters/stats/exports count unique session_ids only — never inflate. Mock and cloud must agree. 2) Codex capture lands separately from my local codex-capture branch — in-scope for the spec, do not build it here. 3) Cursor is official same-tier scope. 4) Yes: live-uploading e2e tests behind explicit opt-in. Note: this fork (ikamensh/gleaner) is hive's working repo for now; PRs here. Update the brief; I approve right after.

assistant:
**Revised Brief**

Mission:
Gleaner is the durable system of record for coding-agent sessions: capture transcripts, scrub sensitive content before upload, store metadata plus raw transcript safely, and make sessions retrievable for audit, search, and lightweight usage insight.

Current scope:
Claude Code, Cursor, and kodo classification are in scope. Cursor is official same-tier support. Codex capture is in scope for the overall spec, but implementation lands separately from your local `codex-capture` branch; do not build the Codex adapter here.

Next iteration:
Harden the core capture contract around idempotent uploads and safe local acceptance testing.

Acceptance direction:
1. A duplicate `session_id` upload uses last-write-wins semantics.
2. The newer upload replaces transcript and metadata for that `session_id`.
3. Counters, stats, and exports count unique `session_id`s only and must never inflate from duplicate uploads.
4. Mock/local storage and cloud DB storage must agree on duplicate behavior.
5. Default CI/local acceptance tests must prove capture, duplicate replacement, unique-session counting, and raw retrieval without touching production.
6. Live-uploading e2e tests stay behind explicit opt-in only; default CI must not create deployed-service data.
7. Docs should reflect actual supported sources and current behavior: Claude Code and Cursor here; Codex capture pending/separate.
8. Code changes should be PR’d against this fork, `ikamensh/gleaner`, as Hive’s working repo for now.

Likely next steps:
1. Write acceptance stories for capture, duplicate replacement, unique stats/counting, exports, and raw retrieval.
2. Add or adjust tests for mock/local duplicate behavior.
3. Fix storage behavior so duplicate `session_id` replacement is consistent across mock and cloud paths.
4. Gate live production e2e tests behind explicit opt-in.
5. Reconcile README/storage/docs with supported-source reality.

Remaining material questions:
None right now.

The user approved the latest intake brief.
```

